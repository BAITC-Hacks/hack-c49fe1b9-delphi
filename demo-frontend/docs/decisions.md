# Решения фронта (demo-frontend)

Журнал решений и проверок фронта. Всё, что касается `demo-frontend/`, пишется здесь и в `demo-frontend/docs/design/`, а не в корневой `docs/`. Время местное (UTC+5), по меткам git.

Документы фронта:

| Файл | О чём |
|---|---|
| [`design/DESIGN.md`](design/DESIGN.md) | Дизайн-система: токены, компоненты, статусы, микротекст |
| [`design/SCENARIOS.md`](design/SCENARIOS.md) | Сценарии пользователя и жюри со статусами реализации |
| [`design/ASTRA-UI-PROMPTS.md`](design/ASTRA-UI-PROMPTS.md) | Промпты для генерации экранов |
| [`../README.md`](../README.md) | Запуск, маршруты, контракт с API бэкенда |

## 23 сентября 2026

| Время | Решение / событие | Коммит |
|---|---|---|
| 15:58 | Корневой `.gitignore` (правило `lib/` из Python-шаблона) скрывал `demo-frontend/src/lib/`: в `main` не было `api.ts`, `status.ts`, `format.ts`, `export.ts`, `utils.ts`, из чистого клона фронт не собирался. Добавлено исключение `!demo-frontend/src/lib/` | `f888fe3` |
| 16:04 | Фронт подключён к гранулярному API (`backend/openapi.json`) без изменений бэкенда. Экраны работают на прежней модели `types.ts`; преобразование — в новом `src/lib/adapter.ts` по таблицам `docs/handoff.md` §3.2–3.3. Офлайн-демо `/analyses/demo` в API не ходит | `8633fd3` |
| 16:04 | Загрузка: create → по одному запросу на файл (видно, как прочитан каждый: ред., фрагменты, предупреждения) → при `partial` явное согласие «Запустить ограниченный анализ» → start. Черновик переиспользуется при повторе, уже загруженные файлы не отправляются второй раз | `8633fd3` |
| 16:04 | Прогресс: отмены в API нет — кнопка «К истории» вместо «Отменить». Упавший или прерванный run не возобновляется: «Повторить анализ» = `repeat` + новый запуск с теми же опциями | `8633fd3` |
| 16:04 | Честность данных: уверенности сопоставления у бэкенда нет — поле стало необязательным, в таблице «—». Трассы инструментов API не отдаёт — панель «Как агент решал» в live скрыта, у «не найденных» функций показано покрытие поиска по комплекту «После» | `8633fd3` |
| 16:10 | Проверка на реальном бэкенде без ключа ИИ (локальный PostgreSQL): transfer (2 файла «После»), DOCX ред. 8/9 → `partial` → согласие → запуск → 503 `ai_not_configured` по-русски. Результаты transfer / missing / overlap сохранены через `persist_result` бэкенда (та же валидация, что у настоящего запуска) и проверены в UI | `84dee8d` |
| 16:10 | Карточка дублирования/конфликта сравнивает две функции «После» (так их требует бэкенд), а не «До» с «После» | `84dee8d` |
| 16:15 | Проверка человеком (сценарии H1–H5): «Подтвердить / Вопрос / Отклонить» + заметка в панели источников, `PUT /api/findings/{id}/review`. Результат пересобирается из сохранённого пакета без перезагрузки; заключение и экспорт учитывают проверку; отклонённые строки приглушены и внизу | `e4af0f2` |
| 16:16 | Ошибки выполнения run переводятся на русский; ошибка повтора показывается отдельно от ошибки опроса | `75e7066` |
| 16:20 | Документы фронта перенесены из `docs/design/` в `demo-frontend/docs/design/`; журнал фронта — этот файл | — |

## Не проверено

- Живой прогон с моделью: нужны `OPENAI_API_KEY` и `OPENAI_MODEL` в `backend/.env`. После — прогнать `backend/fixtures/synthetic/transfer` и ред. 8/9, сверить с `backend/fixtures/synthetic/manifest.json`.
- Отдача `demo-frontend/dist` бэкендом (`StaticFiles`) — зона бэкенда (`docs/handoff.md` §2).
